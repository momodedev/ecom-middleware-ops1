[
  {
    "labels": {
      "job": "kafka-jmx"
    },
    "targets": [
    {% for host in groups['kafka'] | sort %}
      "{{ hostvars[host]['private_ip'] }}:{{ kafka_jmx_exporter_port }}"{% if not loop.last %},{% endif %}
    {% endfor %}
    ]
  }
]
